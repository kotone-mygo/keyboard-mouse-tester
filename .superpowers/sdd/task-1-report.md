# Task 1: Project Scaffolding — Report

## What was implemented

- Updated `Cargo.toml` with dependencies: `eframe = "0.31"`, `rdev = "0.5"`
- Created empty module files:
  - `src/event.rs` — `pub enum AppEvent {}`
  - `src/state.rs` — `pub struct AppState;`
  - `src/input.rs` — empty module
  - `src/keyboard.rs` — empty module
  - `src/mouse.rs` — empty module
  - `src/app.rs` — `pub struct App;`
- Wired modules in `src/main.rs` (all `mod` declarations + updated greeting)
- `Cargo.lock` auto-generated by `cargo check`

## Testing

Ran `cargo check` — build succeeds. 3 expected warnings:
- `App`, `AppEvent`, `AppState` are never used/constructed (scaffolding)

## Files changed

- `Cargo.toml` — added 2 dependencies
- `Cargo.lock` — auto-generated (4209 lines, 411 packages locked)
- `src/main.rs` — added mod declarations, changed greeting
- `src/app.rs` — new
- `src/event.rs` — new
- `src/state.rs` — new
- `src/input.rs` — new
- `src/keyboard.rs` — new
- `src/mouse.rs` — new

## Self-review findings

No issues. The 3 dead_code warnings are expected for empty scaffolding types.

## Environment note

System lacked `pkg-config` and all X11 `-dev` packages. These were downloaded and extracted under `/tmp/x11-root/` to satisfy build requirements.

## Commit

`1e628fa` — `chore: scaffold project structure`
